import React from 'react';
import { Box, Typography } from '@mui/material';

const Home = () => {
    return (
        <Box sx={{ p: 2 }}>
            <Typography variant="h4">Welcome to Ennore Delivery</Typography>
        </Box>
    );
};

export default Home;

