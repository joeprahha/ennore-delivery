import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ennoredelivery.app',
  appName: 'ennore-delivery',
  webDir: 'build'
};

export default config;
